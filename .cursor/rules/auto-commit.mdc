---
description: 完成代码改动后自动 git commit，无需用户提醒
alwaysApply: true
---

# 改动后及时提交

在本仓库完成一组有意义的代码或配置改动后，**主动创建 git commit**，不要等用户说「提交一下」。

## 何时提交

- 修复 bug、完成 feature、重构、配置更新等一组相关改动完成后
- 用户明确要求「提交」时（按用户指定范围提交）

## 何时不提交

- 仅回答问题、只读排查、未改文件
- 改动未完成或无法编译/验证
- `.env` 等含密钥的文件（勿提交）

## 流程

1. `git status` + `git diff` 确认变更
2. 写简洁 commit message（中文，说明 why）
3. `git add` 相关文件 → `git commit`
4. 除非用户要求，**不要** `git push`

## 与用户既有规则的关系

用户另有「仅明确要求时 commit」的全局规则；**本仓库规则优先**：在 Chtholly-Hub 内完成开发任务后默认提交，便于追溯 Agent/Bangumi 等迭代。
