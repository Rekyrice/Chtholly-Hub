---
description: 组件层样式与交互硬约束（禁止 inline 设计值）
globs: apps/web/**/components/**/*
alwaysApply: false
---

# 组件局部硬约束

进入前端组件目录时，同时遵循 [`apps/web/AGENTS.md`](../../apps/web/AGENTS.md)。以下是该 glob 的 Cursor 局部硬约束。

## 禁止 inline 设计值

以下设计属性不得写入 `style={{ ... }}`：

- `color`、`background`、`backgroundColor`、`borderColor`
- `fontSize`、`fontWeight`、`letterSpacing`、`lineHeight`
- `padding`、`margin`、`gap`

改用 Tailwind 工具类、Design Token 或 `globals.css` 中的语义类。inline style 仅允许动态计算值（例如进度、图表尺寸）或第三方库强制要求的 `style` prop；不得借例外硬编码设计值。

## 交互与可访问性

- 所有可点击、可聚焦元素必须提供清晰的 hover、focus-visible 状态与短时 transition。
- 不得移除可见焦点；键盘操作、语义元素/ARIA 和触控目标不得因视觉调整而退化。
- 动效必须遵循 `prefers-reduced-motion`，不得把动画作为理解状态的唯一方式。

## 可测试性与截图

- 新增交互组件优先提供稳定的可访问名称；仅在语义定位不足时添加稳定的 `data-testid`。
- 用 Playwright MCP 覆盖实际交互，并按受影响布局检查桌面与移动视口截图；同时检查键盘焦点和 reduced-motion 行为。
