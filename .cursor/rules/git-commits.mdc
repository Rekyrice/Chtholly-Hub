---
description: Git 提交策略：分阶段、小步、默认自动提交
alwaysApply: true
---

# Git 提交策略

## 默认行为

- **改完就提交**：完成一项任务或一个明显阶段后，默认自动创建 commit。
- **唯一例外**：用户明确说「不要提交」「先别 commit」「等我确认再提交」等。

## 分阶段提交（让历史看起来真实）

不要把互不相关的大改动塞进一次 commit。按任务的**自然阶段**拆分，例如：

| 场景 | 建议拆分方式 |
|------|-------------|
| 功能开发 | 模型/接口 → 服务逻辑 → 前端 UI → 样式微调 |
| Bug 修复 | 根因修复 → 顺带测试/文档（若需要） |
| 资源替换 | 资源文件 → 引用代码 → 构建/脚本 |
| 多模块 | 后端与前端分开；基础设施与业务分开 |

每次 commit 应能独立说清「为什么改」，message 聚焦动机而非文件清单。

## Commit message

- 语言：**中文**
- 格式：**Conventional Commits**（`feat:`、`fix:`、`refactor:`、`chore:`、`docs:`、`test:`）
- 正文 1–2 句，说明「为什么」；避免「更新了若干文件」式空泛描述

## 示例

```
fix(web): 修复侧边栏立绘缩放导致灰底方框

PNG 去预乘白边并修正 1:1 尺寸；呼吸动画移到容器避免线条闪烁。
```

```
fix(server): 为 SeedInteractionService 标注构造器注入

多构造器时 Spring 无法自动选择，导致启动失败。
```

## 不要做的事

- 不要一次 commit 混合无关功能（例如 seed 修复 + 立绘 CSS + 搜索页排版）
- 不要提交 `.env`、密钥、`_incoming/` 临时资源（除非用户明确要求）
- 不要自动 `push`，除非用户明确要求
