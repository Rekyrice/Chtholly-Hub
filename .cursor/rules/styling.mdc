---
description: 前端样式与 Design Token 硬约束（禁止硬编码颜色）
globs: apps/web/**/*.tsx,apps/web/**/*.css
alwaysApply: false
---

# 样式与 Design Token 局部硬约束

编辑前端 TSX/CSS 时，同时遵循 [`apps/web/AGENTS.md`](../../apps/web/AGENTS.md)。以下约束不得被页面或组件局部实现放宽。

## 颜色与 Token

- 颜色字面量仅允许用于 `apps/web/app/globals.css` 中 Design Token 的源定义（例如 `:root`、`@theme inline`）；不得在其他 TSX/CSS 使用点硬编码 hex、rgb/hsl 或任意颜色值（例如 `#009688`、`rgb(...)`、`text-[#424242]`）。
- 除上述 token 源定义外，所有 TSX/CSS 使用点必须引用 `globals.css` 已定义的 CSS 变量或映射到 token 的 Tailwind 类；组件 inline style 同样没有颜色字面量例外。
- 新增或调整 token 时必须同步维护 `:root` 与 `@theme inline`，并使用表达语义而非具体颜色的名称。
- `site.config.ts` 中的主题字段只作既有 SSR 兜底；新代码不得从那里读取视觉颜色。视觉改色统一修改 `globals.css` token。

```tsx
// BAD
<button style={{ background: "#009688", color: "#fff" }} />
<div className="text-[#424242]" />

// GOOD
<button className="bg-[var(--blog-primary)] transition-colors duration-150" />
<p className="text-[var(--foreground)]" />
```

## 动效与可访问性

- hover/focus 状态使用短时 transition，但不得用动画掩盖状态变化或阻塞交互。
- 必须尊重 `prefers-reduced-motion`；减少非必要动画和位移，并保证关闭动效后功能与信息完整。
- 调整颜色 token 时必须检查文本、边框、焦点环与交互状态的对比度，不得以仅靠颜色的方式传达信息。
