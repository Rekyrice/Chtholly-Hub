---
description: Next.js 前端补充约定（编辑 web 代码时生效）
globs: apps/web/**/*
alwaysApply: false
---

# 前端补充（详情见 AGENTS.md）

- 遵守 `apps/web/AGENTS.md`：写路由/Server Component 前查阅 `node_modules/next/dist/docs/`
- 默认 Server Components；仅交互需要时用 `"use client"`；类型在 `lib/types/`
- RSC/SSR 用 `apiClient.ts` + `API_SERVER_URL`（默认 `http://localhost:8888`，**非 8080**）
- 沿用现有 Sakuga 视觉与组件；localStorage 仅放 `useEffect`，避免 hydration 不一致
- 站点配置 `lib/site.config.ts`；产品名 Chtholly Hub，作者 Rekyrice
