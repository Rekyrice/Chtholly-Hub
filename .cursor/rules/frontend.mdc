---
description: Next.js 前端入口规则（编辑 web 文件时生效）
globs: apps/web/**/*
alwaysApply: false
---

# 前端规则入口

进入 `apps/web/**` 后，在修改代码前加载：

1. [`apps/web/AGENTS.md`](../../apps/web/AGENTS.md)：前端局部规则的唯一权威来源。
2. [前端架构](../../docs/architecture/frontend.md)：确认用户路径、真实入口与联动面。
3. 修改 Agent UI、WebSocket、Live2D 或页面上下文时，再读 [Agent 系统](../../docs/architecture/agent-system.md)。

## Cursor 专属行为

- 本项目使用 Next.js 16。编写路由、组件或框架 API 前，先查阅 `apps/web/node_modules/next/dist/docs/` 中与任务相关的当前文档，并遵循弃用提示。
- UI 改动使用 `.cursor/mcp.json` 配置的 **Playwright MCP** 在本地页面执行真实交互、必要的桌面/移动视口截图与可访问性检查；具体组件和样式硬约束由对应 glob 的 `components.mdc`、`styling.mdc` 补充。

其余组件边界、数据调用、类型、品牌和验证命令均以 `apps/web/AGENTS.md` 及其稳定文档入口为准。
