---
description: 项目主规则。开始任何任务前务必先阅读 AGENTS.md。
globs: **/*
alwaysApply: true
---

# Chtholly Hub — Cursor 规则

## 第一原则

**先读 `/AGENTS.md`**（模块地图、架构、编码规范、环境变量）。本文件只补充 AGENTS.md 未覆盖的约束。

## 核心规则

1. **语言**: 与用户对话用中文；Commit message 用中文（Conventional Commits 前缀如 `feat:`、`fix:`）。
2. **注释**: 类/方法 Javadoc 英文；方法体内 WHY 注释中文。详见 AGENTS.md。
3. **异常**: 禁止 `catch (Exception ignored) {}`；每个 catch 至少打日志。
4. **配置**: 禁止硬编码密钥；用 `application.yml` 的 `${VAR:default}`。
5. **范围**: 最小 diff，只改当前任务相关文件；未被要求时不 commit/push/新建 markdown。

## Git 与品牌

- 对外品牌 **Chtholly Hub**；作者名 **Rekyrice**；侧栏/导航用 Rekyrice。

## 完成前验证

- 后端改动：`cd apps/server && mvn compile`（核心逻辑再跑相关 `mvn test`）
- 前端改动：`cd apps/web && npm run build`
- 未验证前不得声称「已完成 / 已通过」

## 按目录补充约定

编辑 `apps/server/**` 或 `apps/web/**` 时，Cursor 会自动加载对应 glob 规则（`backend.mdc` / `frontend.mdc`）。
