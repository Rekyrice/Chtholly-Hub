---
description: 项目主规则。开始任何任务前必须先阅读根目录 AGENTS.md。
globs: "**/*"
alwaysApply: true
---

# Chtholly Hub — Cursor 入口规则

## 唯一权威入口

开始任何任务前，先阅读根目录 [`AGENTS.md`](../../AGENTS.md)。项目事实、编码规范、安全约束、验证命令和完整 Git/worktree 规则均以该文件及其指向的最近一级局部 `AGENTS.md`、稳定文档为准；本文件不复制这些内容。

## Cursor 工作行为

- 与用户对话使用中文；用户明确要求其他语言时除外。
- 保持最小 diff，只修改当前任务直接相关的文件，并保留无关的用户改动。
- 完成前按 [`AGENTS.md`](../../AGENTS.md#验证命令) 选择与改动风险匹配的验证；没有新鲜验证证据时不得声称完成或通过。
- Git 提交的 Cursor 默认行为见 [`git-commits.mdc`](git-commits.mdc)，完整规则仍以根 `AGENTS.md` 为准。

## 按 glob 加载局部规则

Cursor 编辑 `apps/server/**`、`apps/web/**`、前端组件或样式文件时，会按 glob 自动加载对应 MDC。MDC 只负责加载入口与 Cursor 专属约束；进入子树后仍必须阅读其最近一级 `AGENTS.md`，不得把 MDC 当作项目事实来源。
