<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chtholly.post.draftedit.DraftEditPreviewMapper">

    <resultMap id="PreviewResultMap" type="com.chtholly.post.draftedit.DraftEditPreview">
        <id property="id" column="id" />
        <result property="ownerId" column="owner_id" />
        <result property="draftId" column="draft_id" />
        <result property="skillId" column="skill_id" />
        <result property="skillVersion" column="skill_version" />
        <result property="baseContentSha256" column="base_content_sha256" />
        <result property="candidateContent" column="candidate_content" />
        <result property="candidateContentSha256" column="candidate_content_sha256" />
        <result property="previewHash" column="preview_hash" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />
        <result property="expiresAt" column="expires_at" />
        <result property="decidedAt" column="decided_at" />
    </resultMap>

    <insert id="insert" parameterType="com.chtholly.post.draftedit.DraftEditPreview">
        INSERT INTO draft_edit_preview (
            id, owner_id, draft_id, skill_id, skill_version,
            base_content_sha256, candidate_content, candidate_content_sha256,
            preview_hash, status, created_at, expires_at
        ) VALUES (
            #{id}, #{ownerId}, #{draftId}, #{skillId}, #{skillVersion},
            #{baseContentSha256}, #{candidateContent}, #{candidateContentSha256},
            #{previewHash}, #{status}, #{createdAt}, #{expiresAt}
        )
    </insert>

    <select id="findById" resultMap="PreviewResultMap">
        SELECT * FROM draft_edit_preview WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="PreviewResultMap">
        SELECT * FROM draft_edit_preview WHERE id = #{id} FOR UPDATE
    </select>

    <update id="markApplied">
        UPDATE draft_edit_preview
        SET status = 'APPLIED', decided_at = #{decidedAt}
        WHERE id = #{id} AND status = 'PENDING'
    </update>

    <update id="markRejected">
        UPDATE draft_edit_preview
        SET status = 'REJECTED', decided_at = #{decidedAt}
        WHERE id = #{id} AND status = 'PENDING'
    </update>

    <update id="markExpired">
        UPDATE draft_edit_preview
        SET status = 'EXPIRED', decided_at = #{decidedAt}
        WHERE id = #{id} AND status = 'PENDING'
    </update>
</mapper>
