<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chtholly.notification.mapper.NotificationMapper">

    <insert id="insert">
        INSERT INTO notifications (id, user_id, type, payload)
        VALUES (#{id}, #{userId}, #{type}, #{payload})
    </insert>

    <select id="listByUser" resultType="com.chtholly.notification.model.NotificationRow">
        SELECT
            id,
            user_id AS userId,
            type,
            payload,
            read_at AS readAt,
            created_at AS createdAt
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countStatsByUser" resultType="com.chtholly.notification.model.NotificationCountStats">
        SELECT
            COUNT(1) AS total,
            COALESCE(SUM(CASE WHEN read_at IS NULL THEN 1 ELSE 0 END), 0) AS unread
        FROM notifications
        WHERE user_id = #{userId}
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(1) FROM notifications WHERE user_id = #{userId} AND read_at IS NULL
    </select>

    <select id="countUnreadLikePost" resultType="long">
        SELECT COUNT(1)
        FROM notifications
        WHERE user_id = #{userId}
          AND type = #{type}
          AND read_at IS NULL
          AND JSON_UNQUOTE(JSON_EXTRACT(payload, '$.postId')) = CAST(#{postId} AS CHAR)
    </select>

    <select id="findById" resultType="com.chtholly.notification.model.NotificationRow">
        SELECT
            id,
            user_id AS userId,
            type,
            payload,
            read_at AS readAt,
            created_at AS createdAt
        FROM notifications
        WHERE id = #{id}
        LIMIT 1
    </select>

    <update id="markRead">
        UPDATE notifications
        SET read_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND user_id = #{userId} AND read_at IS NULL
    </update>

    <update id="markAllRead">
        UPDATE notifications
        SET read_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId} AND read_at IS NULL
    </update>

    <delete id="deleteExpiredRead">
        DELETE FROM notifications
        WHERE read_at IS NOT NULL
          AND created_at &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

    <delete id="deleteExpiredReadByUser">
        DELETE FROM notifications
        WHERE user_id = #{userId}
          AND read_at IS NOT NULL
          AND created_at &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

</mapper>
