# 本地开发 profile：默认启用 Swagger（可被 SWAGGER_ENABLED 覆盖）
springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLED:true}
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:true}

# 本地冷启动：首次启动自动写入种子（seed_data 幂等标记，重复启动会跳过）
seed:
  enabled: true
  mode: full
